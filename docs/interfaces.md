# Interfaces — Tinyshop

Canonical cross-layer contracts and payload shapes.

## SSE Event Payloads

[FILL: Define SSE event types and payloads once the Architect specs them. Expected events include artifact-change, state-update, bootstrap-progress.]

## API Route Contracts

[FILL: Define Next.js API route request/response shapes. Expected routes include artifact index, launch package assembly, TINY Shopkeeper chat, settings CRUD.]

## Shared Type Definitions

[FILL: Define shared TypeScript interfaces used across frontend and backend. Expected types include Artifact, LaunchPackage, ProjectConfig, DispatchSuggestion.]
